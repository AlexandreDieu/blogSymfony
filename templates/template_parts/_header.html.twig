<header>
	<navclass= class="navbar navbar-expand-lg navbar-dark bg-primary">
		<div class="container-fluid">
			<a class="navbar-brand" href="#">Blog Symfony</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>

			<div class="collapse navbar-collapse" id="navbarColor01">
				<ul class="navbar-nav me-auto">
					<li class="nav-item">
						<a class="nav-link" href="{{ path('main') }}">Articles</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('contact') }}">Contact</a>
					</li>
				</ul>
			</div>
			<div class="navbar-nav me-auto">
				{% if app.user %}
					<div class="nav-item">
						<p class="nav-link">{{ "Bonjour, " ~ app.user.email }}</p>
					</div>
					<div class="nav-item">
						<a href="{{ path('app_logout') }}" class="btn btn-outline-danger" id="navbarColor02">Déconnexion</a>
					</div>
				{% else %}
					<div class="nav-item">
						<a href="{{ path('app_login') }}" class="btn btn-outline-success">Connexion</a>
					</div>
				{% endif %}
			</div>
		</div>
	</navclass="container">
</header>
